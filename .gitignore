# Foundry
contracts/out/
contracts/cache/
contracts/broadcast/

# Node
node_modules/
.env
.env.local

contracts/node_modules/
contracts/package.json
contracts/package-lock.json

web/scripts/_shot.mjs

# promo: binarios de video/imagen no van al repo publico
promo/teaser-pinned/assets/
promo/teaser-pinned/renders/

# media-use: los binarios cacheados en .media/<tipo>/ no van al repo;
# el ledger de texto (index.md, manifest.jsonl) si es fuente versionable.
promo/teaser-pinned/.media/*/
